    //  For Each WorkItemUpdate in Local Store
    //    SendUpdateToServer().then(_ => RemoveWorkItemUpdate)
            Update Types
            WorkItemUpdates
            WorkItemStatus - Completed
            Comments
            Attachments

    //  Next
    //  Once Complete =>
    //
    //  Get all Work Items Assigned to Me (For Today and Tomorrow) - Returns WorkItemStatusReport -- API Done
    //    For Each Work Item In fetched List
    //      Check to see if WorkItem is in Local Store
    //      If I dont Have a copy yet -- (Done below) or WorkItem.LastUpdated > LocalCopy.LastUpdated
    //        GetWorkItemTopLevelDetails() -- API Done
    //    Next

    //  Once Complete =>
    //    For Each Work Item in Local Store
    //      If LocalWorkItem not in downloaded list (Above)
    //        RemoveWorkItem(workItem)

    // Save Store (If Needed)

    //  Once Complete =>
    //    For Each Work Item in Local Store
    //      If StatusReportLines.has(thisWorkItemID)
    //        get SRL = StatusReportLines.find(thisWorkItemID)
    //        If SRL.dateLastUpdated > workItem.LastUpdated
    //          GetWorkItemTopLevelDetails() -- API Done

    //        For Each commentUpdate in SRL.Comments
    //          IF workItem.Comments[id] === null OR commentUpdate.LastUpdated > workItem.Comments[id].LastUpdated
    //          GetWorkItemComment(workItem, commentUpdate.id)

    //        For Each fileUpdate in SRL.Attachments
    //          IF workItem.Attachments[id] === null OR AttachmentUpdate.LastUpdated > workItem.Attachments[id].LastUpdated
    //          GetWorkItemAttachment(workItem, Update.id)

    //        For Each StatusUpdate in SRL.StatusUpdate
    //          IF workItem.StatusUpdates[id] === null OR StatusUpdate.LastUpdated > workItem.StatusUpdates[id].LastUpdated
    //          GetWorkItemStatusUpdate(workItem, Update.id)

    //        Save Store (If Needed)